{
  "name": "mintcode",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-icons.js && node esbuild.config.js",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true});console.log('Cleaned')\"",
    "watch": "node esbuild.config.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "pngjs": "^7.0.0"
  }
}
